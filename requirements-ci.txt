# Direct inputs for regenerating the Python 3.11 cross-platform merge-trust lock:
# uv pip compile requirements-ci.txt --generate-hashes --python-version 3.11 \
#   --universal --output-file requirements-ci.lock
mcp==1.28.1
filelock==3.29.7
onnxruntime==1.23.2
tokenizers==0.22.2
numpy==2.4.6
sqlite-vec==0.1.9
pytest==9.1.1
pytest-asyncio==1.4.0
cryptography==49.0.0
cffi==2.0.0
